/**
 * REST API
 * 
 * fcgi を用いたエンドポイントに該当する。
 * 
 * e.g. compile.
 * g++ -O3 -DDEBUG -std=c++20 -pedantic-errors -Wall -Werror -I../inc/ main.cpp -o ../bin/api
*/
#include <string>



int main(void) {
    puts("=== START REST API");
    puts("END   REST API ===");
    return 0;
}